const scalingFactor = 1024; //in case we want 1000 instead of 1024

export const B = 1;
export const KB = scalingFactor * B;
export const MB = scalingFactor * KB;
export const GB = scalingFactor * MB;


